# Synthetic local-development configuration. Do not reuse these values in production.
JWT_SECRET=replace-with-at-least-32-random-characters
WEB_ORIGIN=http://localhost:3000
JAVA_DETECTOR_URL=http://localhost:8090
JAVA_DETECTOR_REQUIRED=true
POLICY_FILE=policies/default.yaml
NEXT_PUBLIC_DEMO_MODE=true
NEXT_PUBLIC_API_URL=http://localhost:8080

# Optional: exactly 32 random bytes encoded as base64. If omitted, an ephemeral key is used.
# LOCAL_KMS_ROOT_KEY=
